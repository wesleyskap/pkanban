  <div class="top-line">
    <h1></h1>
    <p></p> 
  </div>
  <div class="container">
    <div id="help">
      <h3>Esqueceu sua senha?</h3>
      <p>Caso você tenha esquecido sua senha, basta informar seu email de autenticação que será enviado para sua caixa postas as instruções para redefinição de uma nova senha.</p>
    </div>

    <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
      <%= devise_error_messages! %>

      <div><%= f.label :email %><br />
      <%= f.email_field :email %></div>

      <div><%= f.submit "Enviar" %></div>
    <% end %>

    <%= render "devise/shared/links" %>

</div>
