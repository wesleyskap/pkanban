<p id="notice"><%= notice %></p>
<%= link_to 'New Task', :controller => 'tasks', :action => :new, :id => params[:id] %></td>
<%= link_to 'Back', sprints_path %>

<div id="kanban">

  <div id="coll_01" class="recebeDrag coll">
    <h1>Back Log</h1> 
    <% @coll_01.each do |task| %>
      <div class="item_drag" id="<%= task.id %>">
        <h2 style="background:<%= task.color %>"><%= task.id %> - Priority <%= task.priority %></h2>
        <p><%= task.name %></p>
        <%= link_to 'Edit', :controller => 'tasks', :action => :edit, :id => task.id %>
        <%= link_to 'Delete', task, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
      </div>
    <% end %>
  </div>

  <div id="coll_02" class="recebeDrag coll">
    <h1>In Progress</h1>
     <% @coll_02.each do |task| %>
      <div class="item_drag" id="<%= task.id %>">
        <h2 style="background:<%= task.color %>"><%= task.id %> - Priority <%= task.priority %></h2>
        <p><%= task.name %></p>
        <%= link_to 'Edit', :controller => 'tasks', :action => :edit, :id => task.id %>
        <%= link_to 'Delete', task, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
      </div>
    <% end %>

  </div>

  <div id="coll_03" class="recebeDrag coll">
    <h1>Offside</h1> 
    <% @coll_03.each do |task| %>
      <div class="item_drag" id="<%= task.id %>">
        <h2 style="background:<%= task.color %>"><%= task.id %> - Priority <%= task.priority %></h2>
        <p><%= task.name %></p>
        <%= link_to 'Edit', :controller => 'tasks', :action => :edit, :id => task.id %>
        <%= link_to 'Delete', task, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
      </div>
    <% end %>

  </div>

  <div id="coll_04" class="recebeDrag coll">
    <h1>Done</h1>
     <% @coll_04.each do |task| %>
      <div class="item_drag" id="<%= task.id %>">
        <h2 style="background:<%= task.color %>"><%= task.id %> - Priority <%= task.priority %></h2>
        <p><%= task.name %></p>
        <%= link_to 'Edit', :controller => 'tasks', :action => :edit, :id => task.id %>
        <%= link_to 'Delete', task, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
      </div>
    <% end %>

  </div>

</div>


